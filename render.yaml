services:
  - type: web
    name: crypto-dashboard-api
    env: dotnet
    rootDir: backend/CryptoDashboard.API
    buildCommand: dotnet publish -c Release -o out
    startCommand: dotnet out/CryptoDashboard.API.dll
    envVars:
      - key: ASPNETCORE_URLS
        value: http://+:10000